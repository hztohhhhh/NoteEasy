# Root-relative image reference test

This test file is inside `image-reference-tests`, but the image reference below is written relative to the workspace root:

```
![screenshot](image/screenshot.png)
```

When `D:\Desktop\mark-component` is added as a NoteEasy workspace, preview mode should render this image:

![screenshot](image/screenshot.png)

# #102 标题编号自动接管测试

> 测试目标：验证加载带有原始手写标题编号的 Markdown 后，MarkEasy 能识别编号规则，并由自动编号接管。
>
> 预期结果：
> 1. 打开本文件后，顶部编号模式应自动切换为「按章节」。
> 2. 切换到源码模式后，标题中的 `第一章`、`一、`、`1、` 等手写编号应被移除。
> 3. 预览和大纲中仍应显示自动编号，且编号顺序与原始编号一致。
> 4. 导出 Markdown 后，导出的 `.md` 文件标题中应重新写入完整编号。

## 第一章 总览

这一章用于验证二级标题的 `第X章` 编号是否会被识别并交给自动编号显示。

### 一、测试目标

这一节用于验证三级标题的中文顿号编号是否会被识别。

#### 1、识别已有编号

加载后，源码标题应变成 `#### 识别已有编号`，预览中仍显示自动编号。

#### 2、移除源码编号

加载后，源码标题应变成 `#### 移除源码编号`，预览中仍显示自动编号。

### 二、测试步骤

1. 点击顶部「打开」按钮，选择本文件。
2. 确认编号模式自动变为「按章节」。
3. 切换到「源码」模式。
4. 检查标题源码是否已经移除原始手写编号。
5. 点击 `MD` 导出，检查导出的 Markdown 是否重新包含 `第一章`、`一、`、`1、` 等编号。

#### 1、打开测试文件

这一节用于验证新的四级标题编号会从 `1、` 重新开始。

#### 2、检查自动编号

这一节用于验证四级标题连续编号。

## 第二章 结果验证

这一章用于验证第二个二级标题是否被识别为 `第二章`。

### 一、预览结果

预览区和大纲中应显示自动编号，而不是依赖源码里的手写编号。

#### 1、大纲编号

大纲中的编号应与预览区一致。

#### 2、标题编号

标题前的编号应由 CSS 自动编号显示。

### 二、源码结果

切换到源码后，应该看到类似下面的标题：

```markdown
## 总览
### 测试目标
#### 识别已有编号
## 结果验证
### 预览结果
```

导出 Markdown 后，应该看到类似下面的标题：

```markdown
## 第一章 总览
### 一、测试目标
#### 1、识别已有编号
## 第二章 结果验证
### 一、预览结果
```

## 第三章 结束

如果这一章也能被正确识别和接管，说明连续章节编号处理正常。

### 一、最终检查

最终检查三级标题编号接管。

#### 1、完成

最终检查四级标题编号接管。
